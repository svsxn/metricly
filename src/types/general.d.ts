import { LucideIcon } from "lucide-react";

type Card = {
  Icon: LucideIcon;
  title: string;
  amount: string;
};

type NavItem = {
  Icon: LucideIcon;
  name: string;
  href: string;
};

type Activity = {
  Icon: LucideIcon;
  title: string;
  description: string;
  time: string;
};
